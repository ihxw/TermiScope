1. 对比web端和mobile端，找出差异，以web端为准，对mobile端进行完善，不要修改服务端代码，所有API的请求方式和参数保持不变，接受参数也参考web的方式，



1. 修复mobile中监控界面的错误 WebSocket connection to 'wss://ts.whj.com/api/monitor/stream?token=a47eef96a9905920575a38f9' failed: 
2. 检查mobile中各项功能是否完善，优化用户体验，优化图标显示


1. 修复mobile端的本地化问题，优化mobile的布局，这是一个工具类应用，尽量使用紧凑布局，更多的展示数据，参考web的风格对mobile进行风格改造

1. mobile端 终端界面的主机列表改成卡片模式，对每个卡片给与一定的边框和阴影



1. mobile端里面的终端界面里面去掉 `选择主机` 这一样，如果服务端有返回主机的类型，那么使用不同的图表作为卡片中的图标，如果能检测主机是否在线，可以在卡片合适的位置加一个在线标记，删除右下角浮动的快速连接按钮

1. mobile 的监控界面的卡片也使用一定的边框与阴影，优化卡片布局，给一个合适的卡片宽度，允许一行有多个卡片进行自适应适配，将总量、在线状态直接放到header中 这样可以节省空间

1. mobile的监控卡片去掉圆角

1. mobile的监控卡片去掉 `停止监控`按钮

1. 将mobile的终端界面改成和web一样的风格，下来选择要连接的主机，然后支持tab切换已经连接的主机