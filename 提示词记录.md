1. Web端的SFTP上传时直接写入目标文件，不要先保存到临时文件再转移，另外上传界面要分组显示是上传给哪个服务器的


2. 让监控界面的主机显示卡片支持拖动排序


1. SFTP上传文件的逻辑，直接通过主机将上传流量转发到目标主机，不要经过 TermiScope，不要保存到临时文件再转移，另外，要实时的显示上传进度，文件传输过程 should not be blocked    


1. 在flutter中王全安复刻web端，不要修改服务端的API，也不要修改已有的web端，将web端的界面和逻辑等等完全复刻到flutter，要求所以细节一模一样


逐页拆 Web：模板结构、组件、按钮、弹窗、表格列、空状态、加载状态、错误状态。
逐个映射 Flutter 组件：不是“差不多功能”，而是“同位置、同尺寸、同文案、同状态”。
先建立 Ant Design 风格 Flutter 基础组件：按钮、输入框、表格、卡片、弹窗、标签、工具栏。
再按页面迁移，不能边看边自由发挥。
每页做视觉验收：桌面宽度、移动宽度、空数据、有数据、加载中、错误、暗色模式。